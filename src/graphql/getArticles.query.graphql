query GetArticles($start: Int!, $limit: Int!) {
  articles(pagination: { start: $start, limit: $limit }) {
    documentId
    author {
      name
    }
    cover {
      url
    }
    description
    slug
    title
  }
}
